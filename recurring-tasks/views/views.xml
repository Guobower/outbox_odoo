<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="recurring-tasks.list">
      <field name="name">recurring-tasks list</field>
      <field name="model">recurring-tasks.recurring-tasks</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="recurring-tasks.action_window">
      <field name="name">recurring-tasks window</field>
      <field name="res_model">recurring-tasks.recurring-tasks</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="recurring-tasks.action_server">
      <field name="name">recurring-tasks server</field>
      <field name="model_id" ref="model_recurring-tasks_recurring-tasks"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="recurring-tasks" id="recurring-tasks.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="recurring-tasks.menu_1" parent="recurring-tasks.menu_root"/>
    <menuitem name="Menu 2" id="recurring-tasks.menu_2" parent="recurring-tasks.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="recurring-tasks.menu_1_list" parent="recurring-tasks.menu_1"
              action="recurring-tasks.action_window"/>
    <menuitem name="Server to list" id="recurring-tasks" parent="recurring-tasks.menu_2"
              action="recurring-tasks.action_server"/>
    -->
  </data>
</odoo>